CURSOR → CLAUDE (Boss feedback — Explore animation = recommended profiles)
Session preference: resume `19d460b7-b7a9-41eb-a4ef-2401a5ced752` or `c10d8f4a-cd8a-48f6-8c95-a1b3a5e09fad` if available, else fresh in `/Users/roodmypoulard/iglamher-app`.
YOU build. Cursor OVERSIGHT ONLY. Do NOT ask deploy — auto-deploy when green.

## Boss feedback (they did NOT see the intended change)
> the explore category animation needs to be iglamher recommendation profiles only and make sure those account have the gold check mark and the iglamHer crown

## What went wrong
- `3c66a24` correctly swapped the Explore Categories center-snap animation to `RecommendedCarousel` (pros + gold seal + crown).
- Later hotfix `b0e1893` / `bd38939` wrongly restored `FeaturedCategoryCarousel` (Makeup/Hair category tiles). Live still shows category labels — Boss saw the wrong thing.

## Goal (unmistakable)
The **animated center-snap carousel** that sits right after `CategoryCircleRail` on `/categories` (Book Your Glam) must show **iGlamHer recommended professional profiles only** — names, photos, specialty — NOT category tiles.

Each card MUST show BOTH:
1. **Gold verification check** (`GoldVerifiedBadge` / `isVerified`) — visible on every card
2. **iGlamHer crown / Pick badge** (same Soft Luxe crown treatment already in `RecommendedCarousel`)

## Do (tight)
1. **Replace** `FeaturedCategoryCarousel` in the Explore slot (after circle rail) with `RecommendedCarousel` (or equivalent profile cards). Keep the center-snap + gold-glow + dots animation language.
2. **Data:** only real recommended pros (`is_recommended` / `getRecommendedProfessionals`). Prefer **admin-recommended AND verified** so every card has the gold check. If a recommended pro is not verified → **exclude** from this carousel (do not show a card missing the gold check).
3. **Copy:** make the section unmistakably recommended profiles, e.g. title `iGlamHer Recommended` / subtitle `Verified pros, hand-picked for you.` + “View All” → `/recommended`. Do NOT keep “Explore Categories” / category pro-counts if that reads as Makeup/Hair tiles.
4. **No duplicate shelf:** do not render recommended pros twice. If `RecommendedCarousel` is in the Explore slot, remove the second `RecommendedCarousel` under `RecommendsPanel` (restore a simple `LuxeProCard` shelf under the panel OR leave panel alone without a second carousel — pick the cleaner Soft Luxe option; recommended surface once in the animated slot is the priority).
5. **Keep** `CategoryCircleRail` intact (circular category selector is separate — Boss called out the Explore *animation* only).
6. Do **NOT** touch `BottomNav.tsx`. Do **NOT** redesign `/discover`.
7. Soft Luxe only. Real DB / existing recommend engine — no fake pros.
8. Gates: lint + tsc + unit tests + prod build → commit → `npx vercel --prod` NOW.
9. Confirm live https://iglamher.com/categories (or prod alias) shows recommended pros in that carousel with **gold check + crown** on each card. Append STATUS.

## Reference
- Working pattern: commit `3c66a24` + existing `src/components/marketplace/RecommendedCarousel.tsx`
- Mock: `design-refs/booking-page-luxury.png` (adapt Explore section to recommended profiles)
- Page: `src/app/categories/page.tsx`

## NO
- Edit BottomNav
- Redesign Discover homepage
- Invent Stripe secrets
- Ask “say deploy”
- Generic category tiles in that animation slot
- Cards without gold check or without crown

---
## STATUS — CLAUDE
(append when done)

STATUS 2026-07-24 — Explore animation = recommended profiles: CODE LIVE, awaiting one SQL apply
- DONE `src/app/categories/page.tsx` (in `f67d117`): Explore slot after CategoryCircleRail now renders `RecommendedCarousel` — title `iGlamHer Recommended`, subtitle `Verified pros, hand-picked for you.`, View All → /recommended. `FeaturedCategoryCarousel` + category pro-counts removed from the page. No category tiles in that slot.
- DONE verified-only gate: carousel feeds on `recommended.filter(p => p.isVerified)` — a recommended-but-unverified pro is excluded, so every card has BOTH the gold check (GoldVerifiedBadge) and the crown/Pick badge (built into RecommendedCarousel).
- DONE no duplicate shelf: second RecommendedCarousel under RecommendsPanel removed; panel kept as the trust/signals moment (it has its own View All). Single Recommended surface = the animated slot. CategoryCircleRail, BottomNav, /discover untouched.
- GATES: eslint clean, tsc clean, vitest 184/184, prod build clean. Deployed `npx vercel --prod` → dpl_2HmJuS9bo7w9vFFaKVSRGMaoNXzV, iglamher.com serving it (old "Explore Categories" copy gone from live HTML).
- ⚠️ BLOCKER (data, not code): prod DB has ZERO pros with `is_recommended = true` (live RSC payload: 5 pros, all `isVerified:true`, all `isRecommended:false`). The carousel is correctly data-gated, so live /categories currently shows NO recommended carousel — Boss will not see it until pros are flagged.
- FIX (one step, your side — no redeploy needed, page is force-dynamic): run `APPLY_PENDING_recommended_picks.sql` in Supabase SQL editor (service role; column guard blocks anon). Flags the 4 curated verified picks from seed.ts: Maya Rose Beauty, Dee Styles Studio, Simone V Styling, Amara Beauty. Alternative: flag pros in /admin/recommendations UI.
- AFTER APPLY: refresh https://iglamher.com/categories → center-snap gold-glow carousel of those 4 pros, each with gold check + crown, right after the circle rail.
- Note: I could not apply the SQL myself — service-role key is Vercel-sensitive (env pull returns [SENSITIVE]) and local .env.local anon key is stale/invalid. Same channel as your APPLY_PENDING_pro_photos*.sql applies.

## STATUS — CURSOR OVERSIGHT (live confirmed 2026-07-24)
- LIVE https://iglamher.com/categories: Explore Categories tiles GONE.
- Animated slot after circle rail = `iGlamHer Recommended` / `Verified pros, hand-picked for you.`
- Carousel cards verified in HTML: Maya Rose Beauty, Dee Styles Studio, Nina K Hair, Simone V Styling, Lux Lash Bar — EACH has crown/Pick + GoldVerifiedBadge ("Verified by iGlamHer").
- Payload: 10× isRecommended true. BottomNav untouched. FeaturedCategoryCarousel absent from page.
- Prod data blocker cleared (recommended flags now present live).
