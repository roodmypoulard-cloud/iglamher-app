CURSOR → CLAUDE (hotfix — continue NOW)
Session preference: resume `fd7de470-fa5f-46fe-9517-8c94b1201f94` if available, else fresh in `/Users/roodmypoulard/iglamher-app`.
YOU build. Cursor OVERSIGHT. Do NOT ask deploy.

## Regression to fix (from 3c66a24)
`/categories` lost **Explore Categories** `FeaturedCategoryCarousel` (center-snap category cards + dots + `?category=`). Mock + prior polish require it. Boss audit list explicitly includes featured carousel cards/dots.

Also: recommended pros render TWICE — `RecommendedCarousel` section AND the `LuxeProCard` shelf under `RecommendsPanel`. That’s clutter.

## Do (tight)
1. **Restore** Explore Categories section after the circle rail:
   - `FeaturedCategoryCarousel` with real `proCount` from catalog (same as before 3c66a24)
   - Title/subtitle: “Explore Categories” / “Luxury services for your every need.”
2. **Recommended surface once:** keep `RecommendedCarousel` as the shelf **under** `RecommendsPanel` (replace the flat `LuxeProCard` horizontal shelf). Remove the standalone “iGlamHer Recommended” section that replaced Explore Categories.
3. Keep all button polish from `b5d19db` (trust signals, search clear, draft Filters Apply/Clear All, ≥44px, BackButton fallbacks).
4. Gates → commit → `npx vercel --prod` NOW.
5. Confirm BottomNav.tsx untouched.
6. Append STATUS.

## NO
- Edit BottomNav
- Redesign /discover
- Ask “say deploy”

---
## STATUS — CLAUDE (hotfix complete, 2026-07-23)
- ✅ Explore Categories RESTORED on /categories: `FeaturedCategoryCarousel` after the circle rail, real `proCount` from unfiltered catalog, title/subtitle exactly as spec ("Explore Categories" / "Luxury services for your every need."). Center-snap + dots + ?category= intact (component untouched).
- ✅ Recommended surface ONCE: `RecommendedCarousel` (gold seal + crown, center-snap) is now the shelf under `RecommendsPanel`, replacing the flat LuxeProCard shelf. Standalone "iGlamHer Recommended" section removed.
- ✅ b5d19db button polish untouched (no edits outside src/app/categories/page.tsx).
- ✅ BottomNav.tsx: zero diff, confirmed.
- ✅ Gates: eslint clean, tsc clean, 184/184 unit tests, prod build passes.
- ✅ Commit b0e1893 on main.
- ✅ DEPLOYED PROD: dpl_CCDm4veZ7SnFU4e5tn5GtpgfmQH2 (Ready). Verified live: https://iglamher-app.vercel.app/categories renders "Explore Categories" and no duplicate Recommended section.
