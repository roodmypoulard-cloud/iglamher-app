# CURSOR → CLAUDE — iGlamHer EVERYTHING TO 10/10

Boss: **“i want claude to make everything on this app a 10/10”**

Repo: `/Users/roodmypoulard/iglamher-app` (branch `main` @ `c3bc1e0` + your new work)
YOU build. Cursor OVERSIGHT ONLY — do not wait for Cursor mid-build; finish a coherent tranche, append STATUS, then Cursor re-audits.
Design: **Soft Luxe** (black, espresso, warm rose gold, soft blush, editorial serif, generous spacing). No purple AI defaults.
Money: **Express Connect**; keep **deposit + balance hold/capture**. Never invent Stripe/Supabase secrets.
Auto-deploy when gates are green (`npx vercel --prod`) UNLESS a change is ops-only / needs Boss SQL or live keys first — then list the Boss action clearly and still ship the code.

## DO NOT RE-LITIGATE
Legal **C1–C5 already shipped** (`f76de50`, `c3bc1e0`). Migrations **0032→0033→0034 applied** on prod. Do not reopen address privacy / submit gates / badge overclaim unless you find a regression.

## NORTH STAR
A production-grade Soft Luxe marketplace: honest trust, airtight money path, abuse-resistant mutations, full empty/loading/error coverage, CI green, no dead UI, no overclaims.

---

## TRANCHE 1 — CODE THAT MOVES SCORES TO 10 (do this first)

### A) Payments / webhooks (Trust + Production)
1. Stripe webhook: handle **`account.updated`** (sync Connect flags; don’t rely only on UI refresh).
2. Stripe webhook: handle **`charge.dispute.*`** (create/update dispute records; surface in admin).
3. Update `docs/LAUNCH_OPS.md` webhook event list to match reality (deposit PIs, balance hold/capture, Connect, subscription if any).
4. Unit tests for balance **hold / capture / release** paths (`src/lib/payments/`). Mirror existing payout test style. No invented secrets — mock Stripe.

### B) Abuse controls (Security)
1. Wire **`redisRateLimit`** into `rateLimitGuard` (fail open to in-memory only if Redis missing — document).
2. Apply rate limits to booking create, messaging send, loyalty, referral apply, report — use existing limit constants if present.
3. Referral: **defer welcome credit to first paid/succeeded booking** (Sybil fix). Stop immediate credit on apply. Wire or remove dead `sameDevice` path honestly.

### C) Marketplace integrity (Product)
1. Gate public discoverability: incomplete pros should not appear as bookable “ready” if product rules say so — use/extend `isPubliclyVisible` thoughtfully (active + onboarding complete + approved as required). Soft Luxe empty states when filtered out.
2. Reschedule / `change_requested` UX if status machine already supports it — customer + pro surfaces; Soft Luxe; no dead buttons.

### D) Resilience UX
1. Add route-level `error.tsx` for: book/checkout, messages, admin root (and other high-traffic gaps). Reuse Soft Luxe error patterns.
2. Fill critical missing `loading.tsx` (admin, messages/[id], rewards, legal if missing).
3. Skip-to-main link + landmark/`main` audit on primary layouts (a11y).

### E) Legal Phase 2–3 (next product slice — after A–D green)
Only after Tranche 1 A–D pass gates:
1. License jurisdiction checks (don’t claim “licensed in state X” without data).
2. Booking restrictions where license/location rules require.
3. Admin expiration / home-studio review queues + notifications hooks (email existing paths; no fake push).

### F) Tests + CI
1. Keep lint / tsc / vitest / `next build` green.
2. Add **GitHub Actions CI**: lint + typecheck + unit tests on PR/push to main (Playwright optional/smoke if flaky — don’t block forever).
3. Expand money-path unit tests (item A4). Playwright full deposit→hold→capture only if test mode keys exist in CI — otherwise document manual Boss checklist.

### G) Docs honesty
1. Refresh LAUNCH_OPS webhook list.
2. Append STATUS: score movement, residual debt, Boss ops still required.
3. Log any net-new product ideas in `PRODUCT_DECISIONS.md` — do not invent major features outside spec.

---

## TRANCHE 2 — BOSS OPS (Claude prepares checklists; Boss executes)

You cannot invent secrets. Produce a crisp `OPS_CHECKLIST.md` (or STATUS section) for Boss:
1. Confirm prod migrations through **0034** (already claimed applied — verify once).
2. Stripe Dashboard: enable **Connect 1099 tax reporting**.
3. Stripe webhook endpoint events match LAUNCH_OPS (after you update it).
4. Live/test E2E: deposit → start hold → complete capture → transfer → refund reversal once.
5. Verify `pg_cron` jobs (`VERIFY_CRON.sql`) — expire pending / verifications.
6. Counsel review of `/legal/terms` `/legal/privacy` `/legal/cancellation` before claiming “lawyer-approved.”
7. Recommended $2.99 Stripe Price — only when Boss supplies Price ID; until then keep admin `is_featured` / `is_recommended` manual (PRODUCT_DECISIONS).

---

## OUT OF SCOPE FOR THIS PASS (say so in STATUS, don’t fake it)
- Full native Capacitor + APNs/FCM (dormant by design until keys).
- Counsel-signed legal copy (Boss + lawyer).
- Live money E2E without Boss keys.
- Search rewrite to DB-side ranking at scale (note as debt; don’t boil the ocean).
- ID encryption-at-rest + virus scan (Phase 6 deferred — document only unless Boss says now).

---

## SHIP RULES
1. Small coherent commits with clear why.
2. Soft Luxe everywhere you touch UI.
3. Cents integers; UTC store / local render.
4. Zod on mutations; RLS remains source of truth.
5. Gates: `npm run lint` · `npx tsc --noEmit` · `npm test` · `npm run build`
6. Auto-deploy prod when Tranche 1 code is green and safe.
7. Append STATUS to bottom of this file: what hit 10, what’s still honest debt, live URL.

GO. Make iGlamHer a 10 — code first, ops checklist second, no rubber-stamp claims.

================================================================================
STATUS — Claude, 2026-07-25 (everything-to-10 pass, Tranche 1 A–D + F + G)
================================================================================

ALL GATES GREEN at every commit: npm run lint · npx tsc --noEmit · npm test
(250 passing, +15 new) · npm run build. 11 commits on main since 0f1f6fa.

WHAT MOVED TO 10
- A1/A2: webhook now handles account.updated (Connect flag sync — payout
  eligibility no longer depends on the pro visiting /pro/earnings) and all five
  charge.dispute.* events → stripe_disputes (0035) + admin chargeback queue on
  /admin/disputes with evidence deadlines. Pre-migration the webhook acks +
  logs (no retry storm).
- A3/G1: LAUNCH_OPS webhook list now names all 14 handled events (was 4).
- A4: src/lib/payments/__tests__/balance.test.ts — 15 tests over hold/capture/
  release: idempotency, exact amounts + idempotency keys, decline paths,
  missing-card hard fail, graceful release. First mocked-Stripe/Supabase suite
  (repo previously had zero vi.mock usage).
- B1: rateLimitGuard → Upstash Redis when UPSTASH_* env present (distributed);
  documented degradation ladder (redis error → fail open + log; no redis →
  per-instance memory). Boss action: provision Upstash (OPS_CHECKLIST §3).
- B2: booking create, message send, loyalty redeem, referral apply,
  report/block now rate-limited (message/booking constants existed unused; new
  loyalty/referral/report constants).
- B3: SYBIL FIX — no credit at code-apply. All referral rewards (welcome
  credit, referrer credit, referrer points) grant once in referral/qualify.ts
  on first PAID booking, via status-guarded pending→rewarded flip in the
  payment webhook (covers referred customers AND referred pros). This also
  implemented the referrer payout that was documented-but-never-wired.
  sameDevice/sameIp removed honestly (no fingerprinting exists to feed them).
- C1: isPubliclyVisible now = active AND admin-approved (matches the live
  query). Closed: single-profile fetches (by slug/service) could surface an
  activated-but-unapproved profile. Deliberate: Connect readiness does NOT
  gate visibility — deposits are platform charges; payout waits in the ledger.
- C2: reschedule shipped (0036). Propose (either party, +note, rate-limited) →
  accept (time moves; bookings_no_overlap catches conflicts, 23P01 surfaced as
  friendly copy) / decline / withdraw. Notifications + status events each step.
  Degrades honestly pre-migration ("not available yet" + no dead UI).
- D1/D2: 17 route error.tsx + 7 loading.tsx (Soft Luxe patterns; money-adjacent
  error copy is honest about charge state). Previously 2 error boundaries.
- D3: skip-to-main link (focus-visible) + id="main-content" on all 3 shells.
- F2: .github/workflows/ci.yml — lint/tsc/vitest/build on push+PR to main,
  placeholder env only, no secrets. Proves itself on first push.
- G: OPS_CHECKLIST.md (all Boss-only actions w/ verify steps), VERIFY_CRON.sql,
  PRODUCT_DECISIONS.md entry for this pass.

BOSS OPS REQUIRED (full detail in OPS_CHECKLIST.md)
1. Apply APPLY_PENDING_0035.sql then APPLY_PENDING_0036.sql on prod.
2. Stripe: subscribe webhook to the full event list (LAUNCH_OPS §1); enable
   Connect 1099 reporting; one live E2E deposit→hold→capture→transfer→refund.
3. Supabase: run VERIFY_CRON.sql; optionally provision Upstash Redis env.
4. Counsel review of legal pages before any "lawyer-approved" claim.
5. Regenerate the GitHub PAT with `workflow` scope (current token was
   rejected pushing .github/workflows/ci.yml), then git push origin main —
   all commits from this pass are local until then.

HONEST DEBT / OUT OF SCOPE (unchanged from brief)
- E (legal phase 2–3: license jurisdiction checks, booking restrictions,
  admin expiration/home-studio queues) — NEXT tranche, deliberately not
  half-shipped in this one; A–D now green so it is unblocked.
- Playwright deposit→hold→capture E2E needs test-mode keys (manual checklist
  in OPS_CHECKLIST until then). Search remains TS-side ranking (scale debt,
  noted in code). Native push/Capacitor dormant by design. ID encryption at
  rest deferred (Phase 6).
- Deploy: shipped to prod via npx vercel --prod (URL below). Code is
  prod-safe pre-migrations (0035/0036 features degrade honestly until Boss
  applies the APPLY_PENDING files).
