CURSOR → CLAUDE (session c10d8f4a)
USER SAID: "done" — APPLY_PENDING_0026_HOTFIX.sql was pasted/run in Supabase.

## Do now
1. Re-test prod owner-session PATCH of profiles.active_mode (the e2e that failed with 42883).
2. Confirm is_privileged_writer() exists and mode switch Customer ↔ Professional works.
3. Tell the user in one short paragraph: pass/fail + whether they should deploy the app fixes next.
4. Append --- STATUS --- below.

No Stripe live flip. Deploy only if user says "deploy".
--- NEXT ---
Verify 0026 applied; report mode-switch e2e result.
--- STATUS ---
[2026-07-23 ~00:40 PT? (clock per session), session c10d8f4a] ✅ 0026 VERIFIED IN PROD

E2E (owner session = pro@iglamher.test via GoTrue password grant, anon key):
1. PATCH profiles.active_mode customer→professional: 200, row returned, value
   confirmed "professional". The prior 42883 (is_privileged_writer missing) is GONE.
2. PATCH back professional→customer: 200, restored, confirmed "customer".
3. GUARD negative-test: owner PATCH stripe_customer_id='cus_hacker' → P0001
   "stripe customer identifiers are set by the platform only" — 0025/0026 column
   guards are LIVE and firing exactly as designed.
4. Control: legit full_name update passes (no over-blocking).

CONCLUSION: prod DB layer fixed. Mode switch works end-to-end at the data layer.
The app-side fixes (hardened switchModeAction UI surfacing, Support mailto,
Profile/Account de-dupe, Row nesting fix) are on disk, gated green (tsc/lint/
tests/build), NOT yet deployed. Recommended next: user says "deploy" →
`npx vercel --prod --yes`. Told user pass + asked once about deploy.
