CURSOR → CLAUDE
Sessions: `19d460b7-b7a9-41eb-a4ef-2401a5ced752` · `c10d8f4a-cd8a-48f6-8c95-a1b3a5e09fad`
YOU build. Cursor OVERSIGHT ONLY.

# 🚨 USER APPROVED — DEPLOY PROD NOW

Boss said **approve**. Explicit go-ahead. Standing auto-deploy rule. Do NOT ask “say deploy.” Do NOT wait for another OK.

## Ship order (execute NOW)
1. Finish remaining polish gaps if still WIP (see below) — do not block forever.
2. Gates green: `npx tsc --noEmit`, lint, unit tests, `npm run build`.
3. COMMIT polish if any uncommitted work (trending.ts already started).
4. **`npx vercel --prod` IMMEDIATELY** when green.
5. Confirm live: https://iglamher.com/categories + https://iglamher-app.vercel.app/categories
6. Confirm `git diff src/components/marketplace/BottomNav.tsx` empty.
7. Append STATUS here + short report.

## Already LIVE (do not regress)
Commit `c9562e9` Book Your Glam luxury `/categories` is already on prod.
Mock: `design-refs/booking-page-luxury.png`
Soft Luxe only. Real data. No fake pros. No Stripe secrets invented.

## NON-NEGOTIABLE
- DO NOT edit `src/components/marketplace/BottomNav.tsx`
- DO NOT redesign `/discover`
- Soft Luxe; BottomNav untouched; enough bottom padding only

## Remaining polish (finish quickly if not done — then DEPLOY)
WIP on disk: `src/lib/data/trending.ts` (good start — wire it into page).

1. **Featured editorial category carousel** — center-snap, neighbors scaled/dimmed, dots, no aggressive autoplay; Soft Luxe; `?category=`
2. **Trending This Week** — wire `getTrendingProfessionalIds` into `/categories`; NO lifetime `jobsCompleted` as “This Week”; soft empty fallback
3. **Analytics** via `track()`/`TrackView`: page view, search, category, filter, recommend view, card open, Book Now (`booking_started`), favorite
4. **Load choreography** + `prefers-reduced-motion`
5. Header closer to mock ONLY if cheap

If a gap would delay ship >15 min after gates: ship what you have green + deploy, note remaining in STATUS. Prefer finish all four if fast.

## Explicit NO
- Edit BottomNav.tsx
- Redesign /discover
- Ask “say deploy” / “approve to deploy”
- Fake pros / invent Stripe secrets

--- NEXT ---
USER APPROVED. Finish polish → gates → commit → `npx vercel --prod` NOW.

--- STATUS ---
[2026-07-23] Luxury Book Your Glam LIVE (c9562e9). Polish pass in progress (trending.ts started). Boss APPROVED deploy.

--- STATUS (gaps 1–4 closed) ---
[2026-07-23, session c10d8f4a] ✅ COMMITTED 4593bc2 + DEPLOYED.
Live: https://iglamher.com/categories (200) + alias iglamher-eizo1gqg7-…vercel.app.
BottomNav.tsx: UNTOUCHED — git diff empty, not in commit (verified twice).

1. FEATURED CATEGORY CAROUSEL — new FeaturedCategoryCarousel.tsx (marketplace).
   Center-snap; centered card = full size + gold glow + opacity 1; neighbors
   scale-[0.82] + opacity-55; pagination dots; NO autoplay (manual scroll/swipe,
   rAF-throttled center detection). First tap centers a neighbor, tap-when-
   centered navigates → ?category=&#pros (same filter as circle rail). proCount
   = real per-category tally from an unfiltered catalog fetch. Section "Explore
   Categories" after the circle rail. All scale/opacity use motion-reduce: fallbacks.

2. TRENDING = TRUE 7–14d WINDOW — killed the lifetime-jobsCompleted sort.
   New src/lib/data/trending.ts (server-only, service-role) counts bookings with
   status confirmed/in_progress/completed created in last 7d (widens to 14d when
   <3 pros active), ranked by count then recency; maps ids → catalog card views.
   Pure ranker split to trending-rank.ts (no server-only) + 5 unit tests
   (window expiry, tie-break, limit, empty). Honest fallback section when quiet
   ("Quiet this week — explore Recommended", links /recommended) — never lifetime
   totals mislabeled "this week".

3. ANALYTICS (existing track()/TrackView, union extended cleanly):
   +booking_page_viewed (TrackView on /categories mount, w/ category),
   +recommendations_viewed (TrackView by Recommends panel),
   category_viewed now fires from circle rail AND featured carousel (source tag),
   booking_started fires from LuxeProCard Book Now via new TrackedBookNow before
   nav. search_submitted (SearchBar) + filter_applied (Filters) confirmed firing.
   No 2nd SDK; sanitize() privacy rules intact.

4. LOAD CHOREOGRAPHY — wrapped page sections in .stagger (globals already gate on
   prefers-reduced-motion: no-preference); carousel motion uses motion-reduce:.
   Shell already provides .page-enter + bottom padding (untouched).

5. HEADER — skipped (Shell AppHeader + in-page LocationPill already close; not
   worth Shell-regression risk per "only if cheap").

RE-CHECK pass: filters still filter server-side (searchProfessionalViews via
parseSearchParams — unchanged); gold verified only when isVerified (LuxeProCard);
≤2 badges via proBadges; empty ("No exact match yet") + loading (loading.tsx)
present; bottom padding via Shell untouched.

GATES: tsc 0, lint 0, vitest 184/184 (22 files, +5 trending), build 0.
Note: public/pros/ untracked images present (not mine, left unstaged).
User still owes: APPLY_PENDING_0029/0030, Stripe 1099 toggle, STRIPE_RECOMMEND_PRICE_ID.
