CURSOR → CLAUDE (session 19d460b7 — YOU build; Cursor will NOT Design-Mode edit)

Boss rejected: (1) flat shelves-over-grid as “not creative / not luxury”, (2) gradient-block concept mocks.
Boss said show before live for this design pass. Cursor OVERSIGHT ONLY — do **not** wait for Cursor to edit code.

## Production-safe direction (Cursor picks — build this NOW)
**C “The Lookbook” editorial energy + Boss’s required shelves.**

Must ship on `/categories` (Bookings → New booking):
1. Soft Luxe editorial masthead (serif restraint, rose/gold — **no purple**, no emoji chrome).
2. Full-bleed **cover** = top Recommended pro (or Featured) using real `ProfessionalCard` / real photo — not a gradient block.
3. **Recommended** horizontal ProShelf (real `getRecommendedProfessionals`).
4. **Popular** horizontal ProShelf (real `sort: "reviews"`, dedupe vs Recommended).
5. **Browse by service** — organized categories using real `CategoryTiles` + real `public/brand/categories/*.jpg` photography. Not uniform boring wall — section hierarchy + spacing so it feels editorial.
6. Search retained.
7. Empty states when shelves empty; categories empty state already ok.
8. Mobile-first Soft Luxe; a11y + reduced-motion.

Do **not** invent Occasion taxonomy / new DB. Do **not** invent Stripe secrets. Reuse ProShelf / CategoryTiles / ProfessionalCard.

## Done bar
1. Implement in `src/app/categories/**` (+ small supporting components if needed).
2. Gates: tsc, lint, unit, playwright, build — all green.
3. Commit.
4. Real screenshots → show Boss (preview artifact). **Do not deploy until Boss says approve on THIS new design** (his show-before-live for this visual pass).
5. Append STATUS here with sha + preview link. When Boss approves in chat → `npx vercel --prod` immediately and confirm https://iglamher-app.vercel.app/categories.

## Explicit NO
- Do not hand visual work back to Cursor Design Mode.
- Do not deploy rejected `2e1acae` as-is without the Lookbook luxury pass.
- Do not ask Cursor to write UI.

--- NEXT ---
Build Lookbook-luxury /categories with Recommended + Popular + real category photos. Gates → commit → screenshot preview for Boss. No deploy until Boss approves this version.
--- STATUS ---
(awaiting Claude luxury rebuild)
