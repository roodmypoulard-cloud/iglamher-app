# CURSOR → CLAUDE — iGlamHer EVERYTHING TO 10/10

Boss: **“i want claude to make everything on this app a 10/10”**

Repo: `/Users/roodmypoulard/iglamher-app` (branch `main` @ `c3bc1e0` + your new work)
YOU build. Cursor OVERSIGHT ONLY — do not wait for Cursor mid-build; finish a coherent tranche, append STATUS, then Cursor re-audits.
Design: **Soft Luxe** (black, espresso, warm rose gold, soft blush, editorial serif, generous spacing). No purple AI defaults.
Money: **Express Connect**; keep **deposit + balance hold/capture**. Never invent Stripe/Supabase secrets.
Auto-deploy when gates are green (`npx vercel --prod`) UNLESS a change is ops-only / needs Boss SQL or live keys first — then list the Boss action clearly and still ship the code.

## DO NOT RE-LITIGATE
Legal **C1–C5 already shipped** (`f76de50`, `c3bc1e0`). Migrations **0032→0033→0034 applied** on prod. Do not reopen address privacy / submit gates / badge overclaim unless you find a regression.

## NORTH STAR
A production-grade Soft Luxe marketplace: honest trust, airtight money path, abuse-resistant mutations, full empty/loading/error coverage, CI green, no dead UI, no overclaims.

---

## TRANCHE 1 — CODE THAT MOVES SCORES TO 10 (do this first)

### A) Payments / webhooks (Trust + Production)
1. Stripe webhook: handle **`account.updated`** (sync Connect flags; don’t rely only on UI refresh).
2. Stripe webhook: handle **`charge.dispute.*`** (create/update dispute records; surface in admin).
3. Update `docs/LAUNCH_OPS.md` webhook event list to match reality (deposit PIs, balance hold/capture, Connect, subscription if any).
4. Unit tests for balance **hold / capture / release** paths (`src/lib/payments/`). Mirror existing payout test style. No invented secrets — mock Stripe.

### B) Abuse controls (Security)
1. Wire **`redisRateLimit`** into `rateLimitGuard` (fail open to in-memory only if Redis missing — document).
2. Apply rate limits to booking create, messaging send, loyalty, referral apply, report — use existing limit constants if present.
3. Referral: **defer welcome credit to first paid/succeeded booking** (Sybil fix). Stop immediate credit on apply. Wire or remove dead `sameDevice` path honestly.

### C) Marketplace integrity (Product)
1. Gate public discoverability: incomplete pros should not appear as bookable “ready” if product rules say so — use/extend `isPubliclyVisible` thoughtfully (active + onboarding complete + approved as required). Soft Luxe empty states when filtered out.
2. Reschedule / `change_requested` UX if status machine already supports it — customer + pro surfaces; Soft Luxe; no dead buttons.

### D) Resilience UX
1. Add route-level `error.tsx` for: book/checkout, messages, admin root (and other high-traffic gaps). Reuse Soft Luxe error patterns.
2. Fill critical missing `loading.tsx` (admin, messages/[id], rewards, legal if missing).
3. Skip-to-main link + landmark/`main` audit on primary layouts (a11y).

### E) Legal Phase 2–3 (next product slice — after A–D green)
Only after Tranche 1 A–D pass gates:
1. License jurisdiction checks (don’t claim “licensed in state X” without data).
2. Booking restrictions where license/location rules require.
3. Admin expiration / home-studio review queues + notifications hooks (email existing paths; no fake push).

### F) Tests + CI
1. Keep lint / tsc / vitest / `next build` green.
2. Add **GitHub Actions CI**: lint + typecheck + unit tests on PR/push to main (Playwright optional/smoke if flaky — don’t block forever).
3. Expand money-path unit tests (item A4). Playwright full deposit→hold→capture only if test mode keys exist in CI — otherwise document manual Boss checklist.

### G) Docs honesty
1. Refresh LAUNCH_OPS webhook list.
2. Append STATUS: score movement, residual debt, Boss ops still required.
3. Log any net-new product ideas in `PRODUCT_DECISIONS.md` — do not invent major features outside spec.

---

## TRANCHE 2 — BOSS OPS (Claude prepares checklists; Boss executes)

You cannot invent secrets. Produce a crisp `OPS_CHECKLIST.md` (or STATUS section) for Boss:
1. Confirm prod migrations through **0034** (already claimed applied — verify once).
2. Stripe Dashboard: enable **Connect 1099 tax reporting**.
3. Stripe webhook endpoint events match LAUNCH_OPS (after you update it).
4. Live/test E2E: deposit → start hold → complete capture → transfer → refund reversal once.
5. Verify `pg_cron` jobs (`VERIFY_CRON.sql`) — expire pending / verifications.
6. Counsel review of `/legal/terms` `/legal/privacy` `/legal/cancellation` before claiming “lawyer-approved.”
7. Recommended $2.99 Stripe Price — only when Boss supplies Price ID; until then keep admin `is_featured` / `is_recommended` manual (PRODUCT_DECISIONS).

---

## OUT OF SCOPE FOR THIS PASS (say so in STATUS, don’t fake it)
- Full native Capacitor + APNs/FCM (dormant by design until keys).
- Counsel-signed legal copy (Boss + lawyer).
- Live money E2E without Boss keys.
- Search rewrite to DB-side ranking at scale (note as debt; don’t boil the ocean).
- ID encryption-at-rest + virus scan (Phase 6 deferred — document only unless Boss says now).

---

## SHIP RULES
1. Small coherent commits with clear why.
2. Soft Luxe everywhere you touch UI.
3. Cents integers; UTC store / local render.
4. Zod on mutations; RLS remains source of truth.
5. Gates: `npm run lint` · `npx tsc --noEmit` · `npm test` · `npm run build`
6. Auto-deploy prod when Tranche 1 code is green and safe.
7. Append STATUS to bottom of this file: what hit 10, what’s still honest debt, live URL.

GO. Make iGlamHer a 10 — code first, ops checklist second, no rubber-stamp claims.

================================================================================
STATUS — Claude, 2026-07-25 (everything-to-10 pass, Tranche 1 A–D + F + G)
================================================================================

ALL GATES GREEN at every commit: npm run lint · npx tsc --noEmit · npm test
(250 passing, +15 new) · npm run build. 11 commits on main since 0f1f6fa.

WHAT MOVED TO 10
- A1/A2: webhook now handles account.updated (Connect flag sync — payout
  eligibility no longer depends on the pro visiting /pro/earnings) and all five
  charge.dispute.* events → stripe_disputes (0035) + admin chargeback queue on
  /admin/disputes with evidence deadlines. Pre-migration the webhook acks +
  logs (no retry storm).
- A3/G1: LAUNCH_OPS webhook list now names all 14 handled events (was 4).
- A4: src/lib/payments/__tests__/balance.test.ts — 15 tests over hold/capture/
  release: idempotency, exact amounts + idempotency keys, decline paths,
  missing-card hard fail, graceful release. First mocked-Stripe/Supabase suite
  (repo previously had zero vi.mock usage).
- B1: rateLimitGuard → Upstash Redis when UPSTASH_* env present (distributed);
  documented degradation ladder (redis error → fail open + log; no redis →
  per-instance memory). Boss action: provision Upstash (OPS_CHECKLIST §3).
- B2: booking create, message send, loyalty redeem, referral apply,
  report/block now rate-limited (message/booking constants existed unused; new
  loyalty/referral/report constants).
- B3: SYBIL FIX — no credit at code-apply. All referral rewards (welcome
  credit, referrer credit, referrer points) grant once in referral/qualify.ts
  on first PAID booking, via status-guarded pending→rewarded flip in the
  payment webhook (covers referred customers AND referred pros). This also
  implemented the referrer payout that was documented-but-never-wired.
  sameDevice/sameIp removed honestly (no fingerprinting exists to feed them).
- C1: isPubliclyVisible now = active AND admin-approved (matches the live
  query). Closed: single-profile fetches (by slug/service) could surface an
  activated-but-unapproved profile. Deliberate: Connect readiness does NOT
  gate visibility — deposits are platform charges; payout waits in the ledger.
- C2: reschedule shipped (0036). Propose (either party, +note, rate-limited) →
  accept (time moves; bookings_no_overlap catches conflicts, 23P01 surfaced as
  friendly copy) / decline / withdraw. Notifications + status events each step.
  Degrades honestly pre-migration ("not available yet" + no dead UI).
- D1/D2: 17 route error.tsx + 7 loading.tsx (Soft Luxe patterns; money-adjacent
  error copy is honest about charge state). Previously 2 error boundaries.
- D3: skip-to-main link (focus-visible) + id="main-content" on all 3 shells.
- F2: .github/workflows/ci.yml — lint/tsc/vitest/build on push+PR to main,
  placeholder env only, no secrets. Proves itself on first push.
- G: OPS_CHECKLIST.md (all Boss-only actions w/ verify steps), VERIFY_CRON.sql,
  PRODUCT_DECISIONS.md entry for this pass.

BOSS OPS REQUIRED (full detail in OPS_CHECKLIST.md)
1. Apply APPLY_PENDING_0035.sql then APPLY_PENDING_0036.sql on prod.
2. Stripe: subscribe webhook to the full event list (LAUNCH_OPS §1); enable
   Connect 1099 reporting; one live E2E deposit→hold→capture→transfer→refund.
3. Supabase: run VERIFY_CRON.sql; optionally provision Upstash Redis env.
4. Counsel review of legal pages before any "lawyer-approved" claim.
5. Regenerate the GitHub PAT with `workflow` scope (current token was
   rejected pushing .github/workflows/ci.yml), then git push origin main —
   all commits from this pass are local until then.

HONEST DEBT / OUT OF SCOPE (unchanged from brief)
- E (legal phase 2–3: license jurisdiction checks, booking restrictions,
  admin expiration/home-studio queues) — NEXT tranche, deliberately not
  half-shipped in this one; A–D now green so it is unblocked.
- Playwright deposit→hold→capture E2E needs test-mode keys (manual checklist
  in OPS_CHECKLIST until then). Search remains TS-side ranking (scale debt,
  noted in code). Native push/Capacitor dormant by design. ID encryption at
  rest deferred (Phase 6).
- Deploy: shipped to prod via npx vercel --prod (URL below). Code is
  prod-safe pre-migrations (0035/0036 features degrade honestly until Boss
  applies the APPLY_PENDING files).

================================================================================
STATUS — Claude, 2026-07-25 (Professional Verification System task)
================================================================================
Boss task: "Build a complete professional verification system." AUDIT FIRST:
~95% already existed (0023/0024/0032/0033: wizard w/ gov ID + license + portfolio
+ years + category, statuses persisted, admin queue w/ tabs + signed-URL files +
approve/reject/needs-info + internal notes + audit log, role+passcode admin gate,
gold seal, loading/empty/error states). Built ONLY the real gaps — no duplication:

1. Business name (optional) field added to wizard Basics step. Autosaves via
   draftSchema (max 120); empty keeps the existing default (admin queue and
   public profile rely on business_name always having a value).
2. REJECTED → RESUBMIT path (was a dead end):
   - NEW MIGRATION 0037_resubmit_after_rejection.sql + APPLY_PENDING_0037.sql —
     extends the 0025 column guard to allow owner transition
     rejected -> pending_review (everything else stays admin-only).
     >>> BOSS/CURSOR: apply APPLY_PENDING_0037.sql on prod. Until applied,
     every submit path returns a friendly temporary-update message and remains
     editable until the atomic RPC is installed (no data harm).
   - sectionEditable moved to src/lib/pro/application.ts (pure, shared by wizard
     + server actions; rejected = all sections editable). Unit-tested.
   - submitApplicationAction accepts rejected (counts as resubmission, resets
     flagged docs, clears rejection_reason). Banned still blocked.
   - /pro/apply no longer bounces rejected to the status page; wizard shows a
     rejected banner (reason + "re-upload ID" note, since IDs are purged).
   - /pro/application rejected card now has explicit resubmit instructions +
     "Update & resubmit" button.
3. /pro/profile banner is now status-aware: Verification pending / not submitted
   (link to finish) / needs more info (link to update) / not approved (link to
   reason & resubmit). Was one generic "pending" text for every unverified state.
4. Gold Verified badge consistency: ProfessionalCard (list+grid) and
   /professionals/[slug] header now use the branded GoldVerifiedBadge instead of
   the rose VerifiedIcon (matches LuxeProCard/TrendingStrip/RecommendedCarousel).

GATES: tsc clean · eslint clean · vitest 255/255 (added sectionEditable + 0037
static-surface tests) · next build clean. Not deployed, not committed — awaiting
Cursor review per protocol.
FILES: supabase/migrations/0037_resubmit_after_rejection.sql,
APPLY_PENDING_0037.sql, src/lib/pro/application.ts,
src/lib/pro/application-actions.ts, src/components/pro/ApplicationWizard.tsx,
src/app/pro/apply/page.tsx, src/app/pro/application/page.tsx,
src/app/pro/profile/page.tsx, src/components/marketplace/ProfessionalCard.tsx,
src/app/professionals/[slug]/page.tsx, src/lib/pro/__tests__/application.test.ts

CORRECTION LOOP — Cursor review 1 + 2 (2026-07-25)
- Review 1 rejected with moderation/RLS, autosave race, signed-out redirect, and
  non-atomic resubmission findings. All corrected:
  - 0037 now requires account_status=active in the trigger; profile/document/
    verification-storage owner mutations are active-only at RLS.
  - submit_professional_application RPC atomically locks expected state, changes
    status, increments resubmission count, resets flagged docs, and inserts the
    audit event; rejected resubmission requires a fresh pending ID newer than the
    rejection decision.
  - Wizard drains autosave and saves the latest snapshot before submit; server
    autosave UPDATE is conditional on an editable status.
  - Signed-out /pro/apply redirects to /signin?next=/pro/apply.
  - Government-ID purge failures are returned to admins instead of ignored.
  - Authenticated local RLS cases cover active resubmit, suspended profile/RPC
    denial, and banned document insert/delete denial.
- Review 2 cleared those issues and found one deploy-order Major: ALL submit
  paths now depend on the 0037 RPC. Corrected the user surface so missing
  migration errors return friendly "updates are being installed; work is
  saved" copy instead of raw PostgREST/SQL text.
- HARD DEPLOY GATE: apply APPLY_PENDING_0037.sql to production FIRST, then deploy
  this code. Do not auto-deploy code before migration confirmation. There is no
  unsafe inline fallback because it would reintroduce the race the RPC fixes.

--------------------------------------------------------------------------------
CORRECTION LOOP — Claude, 2026-07-25 (independent fresh-context review round 1)
--------------------------------------------------------------------------------
Two independent reviewer agents (security/correctness + UX/regression) reviewed
the verification-task diff. Findings + fixes (all verified, gates re-run):

FIXED BLOCKERS
B1  saveApplicationDraftAction still refused status 'rejected' (early guard ran
    before sectionEditable) → rejected pros' edits silently never saved while
    the wizard showed editable fields; review step could resubmit UNCHANGED
    data. Fixed: guard now allows draft|needs_more_info|rejected; autosave
    errors now surfaced as a visible role="alert" line (not just "Save failed").
B2  CRITICAL: 0037 was rebuilt from the 0025 guard baseline — `create or
    replace` would have DROPPED the 0028 (is_recommended/recommended_at/until),
    0032 (needs_location_review) and 0033 (identity/license/insurance/
    home_studio/salon badge) guards, letting any pro self-award badges and
    Recommended placement via PostgREST. Rebuilt from the 0033 body + the one
    transition change + active-account requirement
    (coalesce(old.account_status::text,'active') = 'active').
    APPLY_PENDING_0037.sql regenerated to match byte-for-byte.

FIXED MAJORS
M1  Banned+rejected accounts could edit/upload docs (only submit was gated).
    Added isBanned() gate to saveApplicationDraftAction / uploadDocumentAction /
    removeDocumentAction; /pro/apply now ACTS on ensureApplicationRowAction's
    refusal (renders the ban message instead of a wizard that can't save).
M2  Business-name clear was fake-success (kept old value, showed "Saved ✓").
    Now "" is sent explicitly and the server reverts to the profile full name —
    the placeholder promise ("leave blank to use your own name") is now literal.
M3  Three surfaces still used the old VerifiedIcon: services/[id],
    RecommendedProCard, /profile (own). All now GoldVerifiedBadge; /profile's
    customer ID-star kept as distinct glyph, comment corrected.

FIXED MINORS
- Suspended accounts now blocked at submit (decision logged in
  PRODUCT_DECISIONS.md: rejection invites a fix; suspension/ban do not).
- /pro/application rejected copy no longer references a reason that isn't there.
- Long rejection reasons: min-w-0 + break-words on profile banner / wizard
  banner / application reason block.
- /pro/profile needs-info banner links to /pro/application (shows the admin
  note) instead of straight to the wizard; null application read now falls back
  to neutral "pending" copy instead of "not submitted".
- bizname input: autoComplete="organization".

TESTS: 258/258 (added: 0037 baseline-drift assertions for every guarded column,
APPLY_PENDING byte-parity check, action-level rejected-status regression
assertions). tsc clean · eslint clean · next build clean.
NOTE: one build break was introduced+fixed during corrections ("use server"
modules may not export consts — BANNED_MESSAGE un-exported).
Still not committed / not deployed — awaiting Cursor re-review + Boss.
>>> UNCHANGED BOSS ACTION: apply APPLY_PENDING_0037.sql (the REBUILT version).

--------------------------------------------------------------------------------
CORRECTION LOOP ROUND 2 — Claude, 2026-07-25 (supersedes stale claims above)
--------------------------------------------------------------------------------
NOTE: a concurrent session hardened the tree mid-review (0037 RLS active-only
owner policies + atomic submit_professional_application RPC w/ fresh-ID rule;
wizard save-draining; /pro/apply sign-in guard). Round-2 findings below are
against THAT tree; earlier STATUS text describing app-side resubmission_count
and '<> banned' trigger copy is superseded.

FIXED (round-2 findings):
B   Pre-0037 prod regression: submit hard-depended on the 0037 RPC → ALL
    submissions (incl. first-time drafts) would fail until Boss ran the SQL.
    Now: schema-gap → legacySubmitFallback() (the exact pre-0037 path, only for
    draft/needs_more_info which the old trigger permits); rejected-resubmit
    pre-0037 fails with honest "not available just yet" copy.
M1  Suspended fake-success writes: 0037 RLS filters non-active owners to ZERO
    rows with no error. Added accountWriteBlock() (banned+suspended, honest
    copy) to draft-save/upload/remove/ensure, and zero-row .select() checks to
    saveProfileAction, saveAvailabilityAction, saveServiceLocationsAction.
    /pro/apply now refuses suspended accounts with the honest card too.
M2  professional_documents forgery: owner could POST review_status='verified'
    rows via PostgREST → satisfied the approval gate, minted 0033 badges via
    syncVerificationBadges, defeated the RPC fresh-ID check. NEW
    trg_pro_document_insert_guard forces owner inserts pristine
    (pending/null verdict fields/created_at=now()).
M3  Review-metadata forgery / FIFO queue-jump: submitted_at, resubmission_count,
    needs_info_note/sections, rejection_reason now guarded — changeable ONLY
    together with the whitelisted submit transition and only to canonical
    values (submitted_at ≈ now(), counter +0/+1, notes cleared-never-set);
    reviewed_at/reviewed_by never owner-writable. Written to admit exactly what
    the RPC sets (it is SECURITY DEFINER but NOT a privileged writer).
MINORS: storage.objects policy DDL wrapped (insufficient_privilege → NOTICE with
dashboard instructions, rest of paste survives); guard-drift tests pinned to
executable `new.x is distinct from old.x` form (comment mentions no longer
satisfy them); text-field clears now honest (empty string persists; cleared
years → null; cleared business name reverts to own name AND is reflected back
into the input); /pro/apply reuses the pre-ensure read (was double-fetching) and
non-moderation ensure failures get friendly copy + Try again; RPC-copy test
updated for the fallback.
GATES: tsc clean · eslint clean · vitest 262/262 · next build clean.
APPLY_PENDING_0037.sql REGENERATED again — byte-parity test enforces it.
Not committed / not deployed. Boss action unchanged: apply APPLY_PENDING_0037.
